# 혈당 예측 모듈 의존성
# 기존 ai/requirements.txt 에 다음을 추가 (또는 별도 파일로 관리)

# 머신러닝 / 딥러닝
torch>=2.0
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0

# Excel 처리 (Shanghai .xls)
xlrd>=2.0
openpyxl>=3.1

# LLM
anthropic>=0.40

# 유틸
python-dotenv>=1.0
loguru>=0.7
tqdm>=4.65
fastapi
uvicorn
pydantic_settings
# 시각화 / 평가
matplotlib>=3.7

# 시뮬레이터 (직접 호출 필요 시)
# simglucose  # 팀 보강 버전 사용 중이므로 주석 처리
