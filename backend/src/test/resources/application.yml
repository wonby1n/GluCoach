spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/s309
    username: ssafy
    password: ssafy1234
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: testSecretKeyMustBeAtLeast32BytesLongForHS256Algorithm
  access-expiration-ms: 3600000
  refresh-expiration-ms: 2592000000
  issuer: glucofit-backend-test
